---
'expressive-code-color-chips': minor
---

Adds a `languages` option to `pluginColorChips()` for enabling color chips in additional, non-CSS languages. The languages you provide are added to the built-in CSS dialects, for example: `pluginColorChips({ languages: ['metro'] })`.
